# AE_qwen_lessons.md — Qwen hard-won rules

Append a dated bullet whenever a brief was ambiguous, a test/impl approach
failed, or a reviewer corrected the work. Keep each lesson one or two lines.

---

## Standing rules

- Always `conda run -n ebus-cloud-env` — base env lacks packages.
- Surgical changes only (see `CONVENTIONS.md`). Touch only the files the brief
  names. Verbose header comment on every function added/modified.
- If a brief is ambiguous or contradicts the code, stop and write the question
  into the brief under a `## Questions` section; set `Status: BLOCKED`.

## Lessons

_(none yet)_
