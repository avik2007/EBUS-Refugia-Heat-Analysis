# Plan: end-of-session docs — todo top item + full recent-actions record

## Context
User is shutting the machine down for the night and asked: (1) make "review the AWS optimizer-test plan" the top of the todo list; (2) record everything else done this session in recent actions, forgetting nothing. Docs only; no code, no commits, no branches, no deletions. Everything below is verified against the repo state read this session.

Files touched: `argo_claude_actions/AE_claude_todo.md`, `argo_claude_actions/AE_claude_recentactions.md`, `argo_claude_actions/AE_claude_lessons.md`, `argo_gemini_actions/AE_gemini_todo.md` (small note), project `CLAUDE.md` (one rule line), new `CONTEXT.md`, and two memory files (`project_engine_untested.md` fix + new d_0 study memory, + `MEMORY.md` index).

## A. `AE_claude_todo.md` (forward-looking only)
1. **New top item: "[TOP] Review + decide: optimizer multi-start test on AWS (Coiled batch)"** — design (refit the 34 Source-2015 windows freely from 6 starting d_0 values 50/100/200/400/800/1500 km; compare best-of-6 LML vs current single-start (300 km) and vs clamped-sweep max; no change to `analyze_rolling_correlations`; new script `d0_optimizer_restart_test.py`), why Coiled (`coiled batch run`, Coiled 1.132.0, experimental, survives laptop shutdown), steps (script + local one-window check, Coiled smoke job on 2 windows, full job writing CSV to new S3 prefix `s3://argo-ebus-project-data-abm/d0_optimizer_test/`, read results next session), open risks (repo not pip-installable -> packaging may take tries; keep laptop on until job shows running), cost (~1 VM-hour, unmeasured, "a few dollars at most" is an estimate), and the 3 pending decisions (approve design; OK new S3 prefix; or run locally tomorrow at zero cost). Interpretation rule: if multi-start finds much better LML -> optimizer local optima drive part of d_0 scatter; if all starts agree -> weak identification is real and the 40-nat gaps come from calibration.
2. **Rewrite the existing 2026-09-17 d_0 item (now #2)** with a STATUS block: multi-year result (CV pooled 0.76/0.69/0.57; 22/25/9% at lower bound, 15/38/36% at upper), sweep sanity result (2015 Source: clamp holds, LML swing median 9.8 nats, argmax spread 4/2/4/10/8/6, 12/34 windows clamped beats free by >1 nat up to 39.8), and revised "candidate next steps": (a) remaining 32 layer-year sweeps (cost unknown — sanity-run runtime lost with scratchpad; decide all 33 vs subset 2010/2015/2020 x 3 layers), (b) LML-vs-d_0 curve plots (planned in step 6, not yet made), (c) constant/pooled d_0 across layers decision (user goal: d_0 as structural fit-improving parameter, not a reported metric), (d) re-check Skin/Source bounds (Source: 14/34 windows prefer >= 800 km, above the 700 km upper bound, consistent with 38% pinned at 700).
3. **Reconcile stale `[ACTIVE #-2]` engine-test item (annotate, do not delete):** evidence that suites 1-5 exist — Gibbs (13 `test_gibbs_kernel_*`, `test_mlops_foundation.py:1381-1563`), thermodynamics (`test_thermodynamics.py`, 16 tests, commit 9a40339), dispatch contract (`test_run_analysis_dispatch_kwargs_full_unconditional_set` etc.), ERDDAP URL (`test_erddap_url_uses_correct_host_and_encoding`), repo layout (`test_get_project_paths_root_is_repo_root_not_cloud_dir`). Not found by grep: signature-drift test (#6) and what to do with `test_pipeline.py`. Leave those two as the only open sub-items; confirm with user before removing the rest (todo rule: completed work moves to recent actions).
4. **Other forward items to add:** (i) `run_ingestion_pipeline` returns `{}` so manifests record `s3_path: null` and no etag/size — offered to fix, user hasn't answered; (ii) investigate Coiled/Dask `unpack requires a buffer of N bytes` ingest failures (5/29 first-pass failures; root cause unknown; retry fixes it); (iii) decide commit strategy for the large uncommitted set (nothing was committed; no branch created); (iv) pending `[For Antigravity]` science review already on `AE_gemini_todo.md` now has more evidence to attach.

## B. `AE_claude_recentactions.md` — replace/extend the 2026-09-18 entry written earlier today (it says sweep "in progress"; now outdated)
Entry must contain ALL of:
- **Result table** (pooled + per-year CV, % at bounds) and CV convention note (ddof=1: 2015 = 0.70/0.78/0.61 vs 0.67/0.74/0.61 logged 09-17).
- **Bug found + fixed: date plumbing.** Root cause: `run_ingestion_pipeline` (`02_ae_cloud_run.py`) dropped `date_start/date_end`; `get_ae_config` defaulted to registry `californiav3` 2015 window (`ae_utils.py:40`); `run_diagnostic_inspection` (`05_ae_update_tomatern0.5.py`) swallowed them via `**_`; `runner.run_analysis` never passed them. Effect: any non-2015 config silently ran on 2015 data under a different name. Pre-existing 2015 outputs unaffected. Code changes in `02_ae_cloud_run.py` (`run_cloud_pipeline` start_date/end_date; `run_ingestion_pipeline` forwards ISO strings), `05_...py` (date_start/date_end -> get_ae_config), `ebus_core/runner.py` (dispatch_kwargs date_start/date_end).
- **Second code change:** `record['lml'] = gp.log_marginal_likelihood_value_` in `argoebus_gp_physics.py` (all kernel types; final post-calibration GP).
- **Tests (tests-first):** 5 new in `test_mlops_foundation.py` (`test_run_analysis_dispatch_kwargs_include_dates`, `test_run_ingestion_pipeline_forwards_dates`, `test_run_cloud_pipeline_passes_dates_to_get_ae_config`, `test_run_diagnostic_inspection_passes_dates_to_get_ae_config`, `test_analyze_rolling_correlations_records_lml`) + `_load_script` helper; suite `test_mlops_foundation.py + test_thermodynamics.py` = 96 passed. Gotcha: CLI tests use repo-root-relative paths -> run pytest from repo root (5 spurious failures from `ArgoEBUSCloud/`).
- **Ingest:** 2010-2020 x 3 layers, 32 new parquets + existing 2015 = 33 in `s3://argo-ebus-project-data-abm/`. Probe: 2010 d0_100 4m19s (Coiled 3 spot workers, us-east-1, ~4 min cluster startup), 982 rows / 41 floats vs 2015's 1388 / 55; `time_bin` offset 1830 d vs 2015 (matches 1826 d + 10 d bin alignment). 3 parallel per-layer chains for remaining 29 (2015 skipped; I first said 32 — miscount, corrected). 24/29 ok first pass; 5 failed with `unpack requires a buffer of N bytes`; retried all 5 in parallel: 4 ok, d500_1000 2014 failed again and left a partial 8-part/437-row write; rerun alone succeeded (12 parts, 730 rows, 32 floats). Validation: 12 parts each, single write timestamp per parquet (no stale objects), contiguous `time_bin` 4010-8030. Nothing deleted from S3. Registry `AEResults/run_registry.jsonl` 82 lines (44 parsed OK after ingest; re-verify parse at execution). 30 new ingest configs + 33 analysis configs listed by pattern.
- **Analysis re-runs:** 33 Gibbs runs, `_lml` suffix (2015 outputs untouched), `outputs.generate_snapshots/physics_plots: false`; probe 2010 Source 1m49s (~9.5 min CPU, multi-core), deep layers 6-10 min each in 3 parallel chains on the 16-core box; all 33 exit 0; two folders per run (`..._timelsfix[_d1500]_lml` manifest folder + `..._3dgibbs_w45_timelsfix[_d1500]_lml` audit folder).
- **Plots:** `d0_distribution_multiyear.py`; figure `AEResults/aeplots/vertical_delta/d0_distribution_2010_2020.png`; stats `AEResults/aelogs/d0_distribution_multiyear_stats.csv`. Palette blue/orange/aqua validated (`validate_palette.js`: all PASS; aqua contrast 2.74:1 WARN -> relief via direct CV labels + stats CSV). 3 render-inspect iterations fixed label collisions. **Deviation from approved step 7:** built a new script instead of generalizing `analyze_d0_distribution()` (left untouched); one multi-year figure instead of per-year PNGs.
- **Sweep:** `d0_lml_sweep.py`; only 2015 Source run (`AEResults/aelogs/d0_lml_sweep/d0_lml_sweep_d150_400_2015.csv`, 204 rows). Full numbers + interpretation + caveats (calibration blurs LML; 1 layer-year; runtime lost). Plain-language definitions given to user (nats = ln likelihood ratio; free vs clamped; 0.05% = clamp check; 4/2/4/10/8/6 = per-window argmax counts).
- **Corrections to earlier statements:** (1) `AE_gemini_todo.md` d_0 claim was already retracted 09-17 — my "still asserts as proven" was wrong (header still says PROVEN; d_0 bullet retracted); (2) Antigravity's 09-18 log states "flat marginal-likelihood surfaces in d_0" as a finding — it was an untested hypothesis and the sweep does not cleanly support it; (3) the stale engine-test todo item (user caught: "DIDN'T WE ALREADY DO THE GIBBS KERNEL TESTS?").
- **Not done / left open:** remaining 32 sweeps; LML-vs-d_0 curve plots; constant-d_0 decision; `s3_path: null` fix; commits; AWS optimizer test awaiting approval (nothing launched on AWS besides ingests); the working-tree files (`configs/...d500_1000_gibbs_timelsfix.yaml`, `vertical_delta_analysis.py`, and everything above) remain uncommitted on `fix/restore-antigravity-hln-vertical-delta`.
- **Process notes:** plan file `.claude/plans/let-s-do-2010-2020-ingest-majestic-kite.md` (approved); this doc plan file; dual-graph `graph_continue` was never called this session (policy miss).

## C. Lessons + rules (user correction => CLAUDE.md rule requires updating both)
- `AE_claude_lessons.md` new #7: presenting a todo item as pending without checking git/tests (Gibbs tests already existed; todo stale). Rule: before presenting a todo item as pending, check `git log` and grep the tests; cite evidence. Also minor: I over-stated the Gemini-todo state without reading line 17 (add to same lesson).
- Project `CLAUDE.md` Hard-Won Rules: one line with that rule (needs user OK since it edits the project rules file).
- `AE_gemini_todo.md`: append a short "[Claude 2026-09-18 update]" bullet under the existing d_0 retraction with the 2010-2020 CVs and the sweep finding (no rewriting of Antigravity's prose).

## D. Memory
- Fix `project_engine_untested.md` (currently says Gibbs/dispatch/URL/layout tests missing — stale): mark suites 1-5 done, remaining: signature-drift, `test_pipeline.py`.
- New `project_d0_identifiability.md` (status, hypothesis, what's pending, files) + `MEMORY.md` index line.

## E. Session end
- Create `CONTEXT.md` (<20 lines: Current Task / Key Decisions / Next Steps) per project dual-graph policy (file does not exist yet).
- After edits: `graph_register_edit` on changed files if the dual-graph server responds.

## Verification
- Re-read each edited file top-to-bottom; every number above must match the CSV/log evidence (re-run `d0_distribution_multiyear.py` stats print to check table; recount configs/registry lines).
- `git status` before/after: only docs/memory changed by this plan; no commits.
- Todo top item is the AWS review item; completed work is absent from todo.
